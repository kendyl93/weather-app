export const selectCurrentCity = (store) => store.currentWeather.city;
export const selectCurrentWeatherLoading = (store) =>
  store.currentWeather.loading;
export const selectforecat = (store) => store.currentWeather.forecast;
